# Avoid EPERM unlink on stale files under the default %LocalAppData%\npm-cache\_logs
# (common with OneDrive, antivirus, or multiple terminals).
# macOS/Linux: remove the cache= line or use: cache=${HOME}/.npm-cache-e-pharmacy
cache=${LOCALAPPDATA}/npm-cache-e-pharmacy
